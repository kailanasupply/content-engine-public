# docs/

Non-operational reference documents. Not loaded by the engine during generation runs.

| File | Purpose |
|------|---------|
| `Content-Engine-Client-Intake-Form.docx` | Client onboarding form |
| `Content-Engine-Sales-Sheet.pdf` | Overview for prospective clients |

These files belong here, not at root. Root is reserved for files Claude reads during generation.
